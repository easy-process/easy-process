spring:
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:easy_process
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop